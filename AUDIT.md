# 作品集上线检查

## 已经处理

- 页面采用纯静态结构，不需要数据库、Node、后台或构建步骤。
- 5 条作品视频已统一为 15 秒、H.264/MP4，带封面帧和 `faststart`。
- 视频总体积从约 350 MB 降到约 15 MB，适合直接上传静态平台。
- 页面补齐个人介绍、姓名、职位、邮箱、所在地和工作数据。
- 桌面端和移动端均检查过，无横向溢出和浏览器报错。
- 视频卡片支持悬停/进入视口预览，点击可完整播放。
- 已加入基础 SEO、分享卡片、favicon、robots、缓存与安全响应头配置。

## 上线前必须确认

### P0 - 公开托管

当前文件已经具备上线条件，但还没有绑定公开域名。需要执行一次上传：

1. 把 `showreel-ready` 文件夹拖到 Netlify Drop。
2. 平台生成公开链接后直接发给客户。
3. 建议随后把站点名改成容易记忆的名称。

如果直接使用 GitHub Pages，请先确认作品视频可以公开，因为免费方案通常需要公开仓库。

### P0 - 联系方式确认

当前邮箱使用简历中的 `1608341441@qq.com`。如果这不是希望对外展示的业务邮箱，需要在 `index.html` 中统一替换。

## 建议继续提升

### P1 - 精选片段不是完整成片

现在每段视频取的是前 15 秒。它能保证体积和加载速度，但不一定正好是每条片子最强的 15 秒。

后续最好为每条视频确定一个最佳起始时间，例如：

- 小米 YU7 GT：从车辆动态镜头开始
- 198 段子：从笑点进入前一秒开始
- 装修 1：从空间完整构图或动作变化开始

### P1 - 项目信息还不够像作品集

当前卡片主要是标题和一句描述，缺少：

- 客户或项目名称
- 担任角色：剪辑、调色、字幕、包装
- 交付平台与时长
- 目标与实际结果
- 一张更准确的项目封面

这些信息能明显提高专业感，也能帮助潜在客户判断你能做什么。

### P1 - 缺少案例详情

正式作品集通常需要每个项目一个详情区域或独立页面，展示：

- 3 到 5 张关键帧
- 创作背景
- 剪辑思路
- 最终成果

单页版本适合初次展示，但不足以支撑高客单价客户深入了解。

### 已处理 - 分享卡片图片使用绝对地址

已改为 GitHub Pages 的完整公网图片地址，转发链接时可以正常抓取封面。

### P2 - 字体依赖外部服务

当前字体来自 Google Fonts。国内部分地区可能加载慢或失败。正式上线后建议把字体文件下载到本地，减少外部依赖。

### P2 - 数据统计

目前没有访问统计，无法知道：

- 访客来自哪个渠道
- 哪个作品被播放最多
- 访客是否点击邮箱

上线后可以接入隐私友好的统计工具，例如 Umami、Plausible 或百度统计。

### P2 - 邮件联系仍有转化损耗

目前只有邮箱。若面向本地客户，建议补充微信二维码或在线表单；若面向海外客户，建议增加 Vimeo/YouTube 链接。

## 推荐发布顺序

1. 用 Netlify Drop 发布当前版本，先拿到可分享链接。
2. 检查手机打开速度和视频自动播放表现。
3. 修正每条精选视频的 15 秒起点。
4. 补项目角色、客户类型和结果数据。
5. 绑定自定义域名并更新分享卡片绝对地址。
6. 添加访问统计与微信/表单联系方式。
